# Sketch Makefile --------------------------------------------------------

BIN = $(shell basename `pwd`).exe

# Optional Libraries -----------------------------------------------------

SDL       ?= 1

# Test Target options ----------------------------------------------------

TEST_ARGS += 100

# Simduino Magic ---------------------------------------------------------

SIMDUINO  = ../../../Simduino
include $(SIMDUINO)/sketch.mk

# EOF --------------------------------------------------------------------
